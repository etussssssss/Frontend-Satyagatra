<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Keadaan saat tombol ditekan -->
    <item android:state_selected="true" android:drawable="@drawable/btn_klik" />

    <!-- Keadaan default (tidak ditekan) -->
    <item android:drawable="@drawable/border_radius_kotaknopad" />
</selector>

